production: 
  sessions: 
    default: 
      uri: <%= ENV['MONGOLAB_URI'] %>
      options:
        safe: true
        skip_version_check: true

development:

  sessions:

    stat:
      database: statlogin
      hosts:
        - localhost:27017
      options:

    default:
      database: heroku_app16851511
      hosts:
        - localhost:27017
      options:


test:
  sessions:

    stat:
      database: statlogin
      hosts:
        - localhost:27017
      options:
        consistency: :strong
        max_retries: 1
        retry_interval: 0

    default:
      database: heroku_app16851511
      hosts:
        - localhost:27017
      options:
        consistency: :strong
        max_retries: 1
        retry_interval: 0




